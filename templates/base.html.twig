<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        <link rel="stylesheet" href="{{ asset('assets/css/style.css') }}">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-GLhlTQ8iRABdZLl6O3oVMWSktQOp6b7In1Zl3/Jr59b6EGGoI1aFkw7cmDA6j6gD" crossorigin="anonymous">
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.2.1/css/all.min.css" integrity="sha512-MV7K8+y+gLIBoVD59lQIYicR65iaqukzvf/nwasF0nqhPay5w/9lJmVM2hMDcnK1OnMGCdVK+iQrJ7lzPJQd1w==" crossorigin="anonymous" referrerpolicy="no-referrer" />
        
    </head>
    <body  class="home-bg">

        <nav class="navbar navbar-expand-lg bg-transparent shadow p-3">

            <img class="rounded-circle img-logo" src="{{ asset('assets/img/logo.png') }}" alt="Image Logo">
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
            </button>
            {% if app.user %}
                <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
                    <ul class="navbar-nav">
                    
                        <li class="nav-item me-5">
                            <h4>Bienvenu(e) {{ app.user.userIdentifier }}</h4>
                        </li>
                        <li class="nav-item me-3">
                            <a class="nav-link text-success-emphasis" aria-current="page" title="Déconnecxion"
                                href="{{ path('app_logout') }}"><i class="fa-solid fa-right-from-bracket fa-2xl"></i></a>
                        </li>
                    
                    </ul>
                </div>
            {% endif %}
            
            </nav>

            <div class="container">
            {# -----------------Message-----------------#}
                {% for type, messages in app.flashes %}
                    {% for message in messages %}
                        {% if type == 'error' %}
                            <div class="alert alert-warning text-center mx-auto mt-4 w-50" role="alert">
                                <strong>{{ message }}</strong>
                            </div>

                        {% elseif type == 'success' %}
                            <div class="alert alert-success text-center mx-auto mt-4 w-50" role="alert">
                                {{ message }}
                            </div>
                        {% elseif type == 'info' %}
                            <div class="alert alert-info text-center mx-auto mt-4 w-50" role="alert">
                                {{ message }}
                            </div>
                        {% endif %}
                        
                    {% endfor %}
                {% endfor %}
            {# -----------------Fin Message----------------- #}
    
    {% block body %}{% endblock %}
    
    {# -----------------Footer----------------- #}
    <div class="footer">
        <p class="mt-2 text-capitalize bg-transparent shadow-lg p-3">© {{ "now"|date("Y") }} Copyright: <a class="a-footer" href="https://appryll.github.io/Portfolio-NataliaFabiano/"><<|NF|>></a>- Toute reproduction interdite sans l'autorisation de l'auteur -</p>
    </div>
    {# -----------------Fin Footer----------------- #}
    
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js" integrity="sha384-w76AqPfDkMBDXo30jS1Sgez6pr3x5MlQ1ZAGC+nuZB+EYdgRZgiwxhTBTkF7CXvN" crossorigin="anonymous"></script>
    </body>
</html>
